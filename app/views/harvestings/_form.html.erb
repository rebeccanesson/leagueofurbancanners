<%= form_for(@harvesting) do |f| %>
  <% if @harvesting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@harvesting.errors.count, "error") %> prohibited this harvesting from being saved:</h2>

      <ul>
      <% @harvesting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :harvest_id %><br />
    <%= f.number_field :harvest_id %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.number_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :hours %><br />
    <%= f.number_field :hours %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
